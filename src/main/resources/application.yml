spring:
  application:
    name: demo

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username:
  h2:
    console:
      enabled: true
jwt:
  secret: mysecretandstuff
  expiration: 200000